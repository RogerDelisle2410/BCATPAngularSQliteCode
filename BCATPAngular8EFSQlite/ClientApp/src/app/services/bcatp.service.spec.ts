import { TestBed } from '@angular/core/testing';

import { BcatpService } from './bcatp.service';

describe('BcatpService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: BcatpService = TestBed.get(BcatpService);
    expect(service).toBeTruthy();
  });
}); 

import { NavyService } from './bcatp.service';

describe('NavyService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: NavyService = TestBed.get(NavyService);
    expect(service).toBeTruthy();
  });
});

import { DewlineService } from './bcatp.service';

describe('DewlineService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: DewlineService = TestBed.get(DewlineService);
    expect(service).toBeTruthy();
  });
});

//import { PinetreeService } from './bcatp.service';

//describe('PinetreeService', () => {
//  beforeEach(() => TestBed.configureTestingModule({}));

//  it('should be created', () => {
//    const service: PinetreeService = TestBed.get(PinetreeService);
//    expect(service).toBeTruthy();
//  });
//});

//import { MidCanadaService } from './bcatp.service';

//describe('MidCanadaService', () => {
//  beforeEach(() => TestBed.configureTestingModule({}));

//  it('should be created', () => {
//    const service: MidCanadaService = TestBed.get(MidCanadaService);
//    expect(service).toBeTruthy();
//  });
//});

//import { AirforceService } from './bcatp.service';

//describe('AirforceService', () => {
//  beforeEach(() => TestBed.configureTestingModule({}));

//  it('should be created', () => {
//    const service: AirforceService = TestBed.get(AirforceService);
//    expect(service).toBeTruthy();
//  });
//});

//import { ArmyService } from './bcatp.service';

//describe('ArmyService', () => {
//  beforeEach(() => TestBed.configureTestingModule({}));

//  it('should be created', () => {
//    const service: ArmyService = TestBed.get(ArmyService);
//    expect(service).toBeTruthy();
//  });
//});


